describe("Filling Survey Form", () => {
  it("should fill the form and display a modal", () => {
    // cy.visit("/survey");
    // cy.url().should("include", "/sign-in");
    // cy.contains("h1", "Sign in");
    // cy.get(".cl-signIn-root").should("exist");
    // cy.get("input[name=identifier]").type(Cypress.env("test_user"));
    // cy.get(".cl-formButtonPrimary").contains("button", "Continue").click();
    // cy.get("input[name=password]").type(Cypress.env("test_password"));
    // cy.get(".cl-formButtonPrimary").contains("button", "Continue").click();
    // cy.visit("/");
    // cy.url().should("include", "/survey");
    // cy.get("button[name=submit-survey-btn]").should("be.disabled");
    // cy.get("button[name=pink]").click();
    // cy.get('button[name="Content Creator"]').click();
    // cy.get("button[name=submit-survey-btn]").should("not.be.disabled").click();
    // cy.get("[data-cy=survey-thankyou-modal]").should("exist");
    // cy.get("a[data-cy=continue-to-templates-btn]").click();
    // cy.url().should("include", "/templates");
  });
});
